//const ipServidor = "https://de10.app/backend";
const ipServidor = "http://localhost/dashboard/public"; // IP local de tu PC

const config = {
  apiBaseUrl: `${ipServidor}/api`,
  imageBaseUrl: `${ipServidor}/storage/imgs`,
};


export default config;
